## Unit Conversion

[Units](https://docs.ethers.org/v5/api/utils/display-logic/#display-logic--units)

```ts filename="Units"
ethers.parseEther(ether: string) ⇒ bigint // Converts 1 to 1_000_000_000_000_000_000
ethers.formatEther(wei: BigNumberish) ⇒ string // Converts 1_000_000_000_000_000_000 to 1

ethers.parseEther("1"); // 1_000_000_000_000_000_000

const balance = await ethers.provider.getBalance(deployerAddress);
ethers.formatEtherformatEther(balance) // 1

{ value: ethers.parseEther("1") }

function parseEth(ether: string): bigint {
  return ethers.parseEther(ether);
}
```
